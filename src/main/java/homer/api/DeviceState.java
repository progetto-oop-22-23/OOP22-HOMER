package homer.api;

/**
 * The state that gets passed to and from the views.
 * Each view/model element expects classes implementing this interface, and
 * will throw an exception if the class is not the expected type.
 */
public interface DeviceState {

}
