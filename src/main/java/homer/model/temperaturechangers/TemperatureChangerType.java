package homer.model.temperaturechangers;

/**
 * Models the types a {@link TemperatureChanger} can assume.
 */
public enum TemperatureChangerType {
    /** The temperatureChanger is a heating device. */
    HEATING,
    /** The temperature changer is an air conditioning device. */
    AIRCONDITIONING
}
